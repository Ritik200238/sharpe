{
  "name": "sharpe",
  "private": true,
  "version": "0.1.0",
  "description": "The autonomous in-play market maker for World Cup odds, with an on-chain book that can't be faked.",
  "workspaces": [
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "recorder": "npm run --workspace services/recorder",
    "web": "npm run dev --workspace apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
